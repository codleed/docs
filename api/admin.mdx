---
title: Admin API
description: Administrative endpoints for content management, user moderation, analytics, notifications, and settings.
---

## Overview

Admin endpoints provide full platform management capabilities. All admin endpoints require authentication with an `admin` role.

<Warning>
Every admin endpoint enforces role-based access via `requireAdminRole`. Non-admin users receive `403 Forbidden`. All mutating actions are logged to the audit trail.
</Warning>

---

## Dashboard overview

Retrieve high-level platform KPIs and recent admin activity.

```bash
GET /api/admin/overview
```

**Auth**: Admin

### Query parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `windowDays` | `7` \| `30` \| `90` | `30` | Time window for metrics |

### Response

```json
{
  "windowDays": 30,
  "kpis": {
    "openModerationFlags": 3,
    "suspendedUsers": 1,
    "failedAdminActionsLast24h": 0,
    "notificationsSentInWindow": 5
  },
  "alerts": {
    "showHighPriorityBanner": false,
    "reasons": []
  },
  "recentActivity": [
    {
      "id": 1,
      "scope": "content",
      "action": "Publish chapter",
      "target": "Physics - Physical Quantities",
      "status": "success",
      "message": "Chapter published successfully.",
      "actor": {
        "id": "...",
        "name": "Admin User"
      },
      "occurredAt": "2026-03-28T10:00:00.000Z"
    }
  ]
}
```

<Accordion title="Alert thresholds">
- **Open moderation flags**: High-priority banner if >= 10 open flags
- **Failed admin actions (24h)**: High-priority banner if >= 5 failed actions in the last 24 hours
</Accordion>

---

## Analytics overview

Retrieve detailed analytics with subject-level performance breakdowns.

```bash
GET /api/admin/analytics/overview
```

**Auth**: Admin

### Query parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `windowDays` | `7` \| `30` \| `90` | `30` | Time window for analytics |

### Response

```json
{
  "windowDays": 30,
  "summary": {
    "activeStudents": 142,
    "quizAttempts": 1250,
    "averageQuizScorePercent": 72.4,
    "threadsCreated": 38,
    "openModerationFlags": 3
  },
  "subjectPerformance": [
    {
      "subjectId": 1,
      "subjectName": "Physics",
      "grade": "9",
      "boardName": "Punjab Board",
      "attempts": 320,
      "averageScorePercent": 68.5,
      "activeStudents": 45
    }
  ]
}
```

---

## Content management

### Get curriculum tree

Retrieve the full curriculum hierarchy: boards > classes > subjects > chapters.

```bash
GET /api/admin/content/curriculum
```

**Auth**: Admin

### Response

```json
{
  "boards": [
    {
      "id": 1,
      "name": "Punjab Board",
      "slug": "punjab",
      "classes": [
        {
          "id": 1,
          "name": "9th",
          "slug": "9",
          "subjects": [
            {
              "id": 1,
              "name": "Physics",
              "slug": "physics",
              "icon": null,
              "description": "Introduction to physics",
              "chapters": [
                {
                  "id": 1,
                  "chapterNumber": 1,
                  "title": "Physical Quantities",
                  "slug": "physical-quantities",
                  "isPublished": true
                }
              ]
            }
          ]
        }
      ]
    }
  ]
}
```

### Create board

```bash
POST /api/admin/content/boards
```

**Auth**: Admin

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | string | Yes | Board display name (min 2 chars) |
| `slug` | string | Yes | URL-safe slug (min 2 chars) |

### Update board

```bash
POST /api/admin/content/boards/:id/update
```

### Delete board

```bash
POST /api/admin/content/boards/:id/delete
```

### Create class

```bash
POST /api/admin/content/classes
```

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `boardId` | number | Yes | Parent board ID |
| `name` | string | Yes | Class name (min 1 char) |
| `slug` | string | Yes | URL-safe slug (min 1 char) |

### Update class

```bash
POST /api/admin/content/classes/:id/update
```

### Delete class

Deletes the class and all its subjects in a transaction.

```bash
POST /api/admin/content/classes/:id/delete
```

### Create subject

```bash
POST /api/admin/content/subjects
```

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `boardClassId` | number | Yes | Parent class ID |
| `name` | string | Yes | Subject name (min 1 char) |
| `slug` | string | Yes | URL-safe slug (min 1 char) |
| `icon` | string | No | Icon identifier |
| `description` | string | No | Subject description |

### Delete subject

```bash
POST /api/admin/content/subjects/:id/delete
```

### Create chapter

```bash
POST /api/admin/content/chapters
```

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `subjectId` | number | Yes | Parent subject ID |
| `chapterNumber` | number | Yes | Chapter number (positive integer) |
| `title` | string | Yes | Chapter title (min 1 char) |
| `slug` | string | Yes | URL-safe slug (min 1 char) |
| `summary` | string | Yes | Chapter summary markdown (min 1 char) |
| `isPublished` | boolean | No | Publish status (default: `false`) |

### Get chapters list

```bash
GET /api/admin/content/chapters
```

### Get chapters graph

```bash
GET /api/admin/content/chapters/graph?q=searchterm
```

Returns a graph of chapter nodes and wiki-link edges.

### Update chapter

```bash
POST /api/admin/content/chapters/:id/update
```

### Delete chapter

```bash
POST /api/admin/content/chapters/:id/delete
```

### Rename chapter

```bash
POST /api/admin/content/chapters/:id/rename
```

| Field | Type | Required |
|-------|------|----------|
| `title` | string | Yes |
| `slug` | string | Yes |

### Publish/unpublish chapter

```bash
POST /api/admin/content/chapters/:id/publish
```

| Field | Type | Required |
|-------|------|----------|
| `isPublished` | boolean | Yes |

### Get chapter summary

```bash
GET /api/admin/content/chapters/:id/summary
```

### Update chapter summary

```bash
POST /api/admin/content/chapters/:id/summary
```

| Field | Type | Required |
|-------|------|----------|
| `summary` | string | Yes (min 1 char) |

### Get chapter links

```bash
GET /api/admin/content/chapters/:id/links
```

Returns outgoing wiki-links and backlinks for the chapter.

### Get link suggestions

```bash
GET /api/admin/content/chapters/link-suggestions?q=searchterm&limit=20
```

### Create exercise

```bash
POST /api/admin/content/exercises
```

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `chapterId` | number | Yes | Parent chapter ID |
| `exerciseNumber` | string | Yes | Exercise identifier (e.g., `"1.1"`) |
| `question` | string | Yes | Question text (min 1 char) |
| `solution` | string | Yes | Solution text (min 1 char) |
| `difficulty` | `"easy"` \| `"medium"` \| `"hard"` | No | Default: `"medium"` |
| `type` | `"mcq"` \| `"short"` \| `"long"` \| `"numerical"` | No | Default: `"short"`. Numerical is only allowed for Physics chapters. |

### List exercises

```bash
GET /api/admin/content/exercises?chapterId=5
```

### Update exercise

```bash
POST /api/admin/content/exercises/:id/update
```

### Delete exercise

```bash
POST /api/admin/content/exercises/:id/delete
```

---

## Quiz management

### Upsert quiz

Creates a quiz if none exists for the chapter, otherwise updates it.

```bash
POST /api/admin/content/quizzes
```

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `chapterId` | number | Yes | Parent chapter ID |
| `duration` | number | No | Duration in seconds |
| `totalMarks` | number | No | Total marks for the quiz |

### Get quiz by chapter

```bash
GET /api/admin/content/quizzes?chapterId=5
```

### Update quiz

```bash
POST /api/admin/content/quizzes/:id/update
```

### Delete quiz

```bash
POST /api/admin/content/quizzes/:id/delete
```

---

## Quiz question management

### Add question

```bash
POST /api/admin/content/quiz-questions
```

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `quizId` | number | Yes | Parent quiz ID |
| `questionText` | string | Yes | Question text |
| `optionA` | string | Yes | Option A |
| `optionB` | string | Yes | Option B |
| `optionC` | string | Yes | Option C |
| `optionD` | string | Yes | Option D |
| `correctOption` | `"a"` \| `"b"` \| `"c"` \| `"d"` | Yes | Correct answer |
| `explanation` | string | No | Explanation (nullable) |
| `marks` | number | No | Marks for this question (default: 1) |

<Note>Adding a question automatically recalculates the quiz's `totalMarks`.</Note>

### List questions

```bash
GET /api/admin/content/quiz-questions?quizId=5
```

### Update question

```bash
POST /api/admin/content/quiz-questions/:id/update
```

### Delete question

```bash
POST /api/admin/content/quiz-questions/:id/delete
```

---

## Flashcard management

### Create flashcard

```bash
POST /api/admin/content/flashcards
```

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `chapterId` | number | Yes | Parent chapter ID |
| `front` | string | Yes | Front side text |
| `back` | string | Yes | Back side text |

<Note>Flashcards are auto-appended with the next `orderIndex`.</Note>

### List flashcards

```bash
GET /api/admin/content/flashcards?chapterId=5
```

### Update flashcard

```bash
POST /api/admin/content/flashcards/:id/update
```

### Delete flashcard

```bash
POST /api/admin/content/flashcards/:id/delete
```

### Reorder flashcards

```bash
POST /api/admin/content/flashcards/reorder
```

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `flashcardIds` | number[] | Yes | All flashcard IDs for the chapter in desired order |

<Warning>The `flashcardIds` array must include ALL flashcard IDs for the chapter. Omitting any ID will cause the reorder to fail.</Warning>

---

## Job management

### Get queue stats

```bash
GET /api/admin/jobs/stats
```

Returns queue statistics (waiting, active, completed, failed) per queue.

### Response

```json
{
  "queues": {
    "analytics": {
      "waiting": 0,
      "active": 0,
      "completed": 15,
      "failed": 0
    },
    "email": {
      "waiting": 0,
      "active": 0,
      "completed": 3,
      "failed": 0
    },
    "cleanup": {
      "waiting": 0,
      "active": 0,
      "completed": 45,
      "failed": 0
    }
  }
}
```

### Retry failed job

```bash
POST /api/admin/jobs/:queueName/:jobId/retry
```

| Parameter | Type | Description |
|-----------|------|-------------|
| `queueName` | string | Queue name (analytics, email, cleanup) |
| `jobId` | string | Job ID to retry |

---

## User management

### List users

```bash
GET /api/admin/users
```

**Auth**: Admin

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `page` | number | `1` | Page number |
| `pageSize` | number | `20` | Results per page (1–100) |
| `q` | string | `""` | Search by name or email |
| `role` | `"student"` \| `"admin"` | — | Filter by role |
| `status` | `"active"` \| `"suspended"` | — | Filter by status |

### Response

```json
{
  "entries": [
    {
      "id": "...",
      "name": "Ahmed Khan",
      "email": "ahmed@example.com",
      "role": "student",
      "status": "active",
      "suspendedAt": null,
      "suspendedReason": null,
      "suspendedBy": null,
      "createdAt": "2026-01-15T10:00:00.000Z"
    }
  ],
  "total": 150,
  "page": 1,
  "pageSize": 20,
  "hasMore": true
}
```

### Update user role

```bash
POST /api/admin/users/:id/role
```

| Field | Type | Required |
|-------|------|----------|
| `role` | `"student"` \| `"admin"` | Yes |

<Note>Admins cannot change their own role (self-mutation is blocked).
</Note>

### Suspend/Reactivate user

```bash
POST /api/admin/users/:id/suspension
```

<AccordionGroup>
  <Accordion title="Suspend">
    ```json
    {
      "action": "suspend",
      "reason": "Violation of community guidelines — minimum 10 characters"
    }
    ```
  </Accordion>
  <Accordion title="Reactivate">
    ```json
    {
      "action": "reactivate"
    }
    ```
  </Accordion>
</AccordionGroup>

Only student users can be suspended. Admins cannot suspend themselves.

---

## Moderation

### List moderation flags

```bash
GET /api/admin/moderation/flags
```

**Auth**: Admin

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `page` | number | `1` | Page number |
| `pageSize` | number | `20` | Results per page (1–100) |
| `status` | `"open"` \| `"resolved"` | `"open"` | Filter by status |
| `targetType` | `"thread"` \| `"reply"` \| `"chapter"` | — | Filter by target type |

### Resolve moderation flag

```bash
POST /api/admin/moderation/flags/:id/resolve
```

| Field | Type | Required |
|-------|------|----------|
| `note` | string | Yes (min 10 chars) |

### Response

```json
{
  "flag": {
    "id": "...",
    "createdAt": "2026-03-25T10:00:00.000Z",
    "targetType": "thread",
    "targetId": "...",
    "targetLabel": "Inappropriate thread title",
    "reason": "Spam content detected",
    "status": "resolved",
    "resolvedBy": "...",
    "resolvedAt": "2026-03-28T10:00:00.000Z",
    "resolutionNote": "Thread removed and user warned."
  }
}
```

---

## Community management

### List threads (admin view)

```bash
GET /api/admin/community/threads
```

**Auth**: Admin

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `page` | number | `1` | Page number |
| `pageSize` | number | `20` | Results per page (1–100) |
| `solved` | `"all"` \| `"solved"` \| `"unsolved"` | `"all"` | Filter by solved status |
| `pinned` | `"all"` \| `"pinned"` \| `"unpinned"` | `"all"` | Filter by pinned status |
| `flagState` | `"all"` \| `"openFlags"` \| `"noOpenFlags"` | `"all"` | Filter by open moderation flags |

### Pin/unpin thread

```bash
POST /api/admin/forum/threads/:threadId/pin
```

| Field | Type | Required |
|-------|------|----------|
| `isPinned` | boolean | Yes |

---

## Notifications

### List notifications

```bash
GET /api/admin/notifications
```

**Auth**: Admin

| Parameter | Type | Default |
|-----------|------|---------|
| `page` | number | `1` |
| `pageSize` | number | `20` |

### Create notification

```bash
POST /api/admin/notifications
```

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `title` | string | Yes | Notification title (min 5 chars) |
| `message` | string | Yes | Notification body (min 10 chars) |
| `audience` | `"all"` \| `"students"` \| `"admins"` | Yes | Target audience |

### Response

```json
{
  "notification": {
    "id": "...",
    "title": "Scheduled Maintenance",
    "message": "The platform will be down for maintenance on Saturday.",
    "audience": "all",
    "status": "sent",
    "createdBy": {
      "id": "...",
      "name": "Admin User"
    },
    "createdAt": "2026-03-28T10:00:00.000Z"
  }
}
```

---

## Settings

### List settings

```bash
GET /api/admin/settings
```

**Auth**: Admin

### Response

```json
{
  "entries": [
    {
      "key": "forum_auto_lock_hours",
      "value": "72",
      "description": "Hours before an inactive thread is automatically locked",
      "updatedBy": {
        "id": "...",
        "name": "Admin User"
      },
      "updatedAt": "2026-03-20T10:00:00.000Z"
    },
    {
      "key": "quiz_pass_threshold_percent",
      "value": "50",
      "description": "Minimum percentage to pass a quiz",
      "updatedBy": null,
      "updatedAt": "2026-01-01T00:00:00.000Z"
    },
    {
      "key": "maintenance_banner_enabled",
      "value": "false",
      "description": "Show maintenance banner to all users",
      "updatedBy": null,
      "updatedAt": "2026-01-01T00:00:00.000Z"
    }
  ],
  "total": 3,
  "page": 1,
  "pageSize": 20,
  "hasMore": false
}
```

### Update setting

```bash
POST /api/admin/settings/:key
```

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `value` | string | Yes | New setting value (1–2000 chars) |

Only the following keys are updatable:

| Key | Description |
|-----|-------------|
| `forum_auto_lock_hours` | Hours before inactive threads auto-lock |
| `quiz_pass_threshold_percent` | Minimum percentage to pass a quiz |
| `maintenance_banner_enabled` | Show maintenance banner (`"true"` / `"false"`) |

### Error responses

| Status | Condition |
|--------|-----------|
| `400` | Invalid setting key or value |
| `404` | Setting key not found (not in updatable list) |

---

## Formulas Management

### List formulas

```bash
GET /api/admin/content/formulas
```

**Auth**: Admin

### Query parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `subjectId` | number? | Filter by subject |
| `chapterId` | number? | Filter by chapter |

### Response

```json
{
  "formulas": [
    {
      "id": 1,
      "subjectId": 5,
      "chapterId": 3,
      "name": "Newton's Second Law",
      "formulaLatex": "F = ma",
      "description": "Force equals mass times acceleration",
      "variables": ["F", "m", "a"],
      "tags": ["mechanics", "force"],
      "createdAt": "2026-03-20T10:00:00.000Z",
      "updatedAt": "2026-03-20T10:00:00.000Z",
      "subjectName": "Physics",
      "chapterTitle": "Newton's Laws"
    }
  ],
  "total": 25
}
```

### Create formula

```bash
POST /api/admin/content/formulas
```

**Auth**: Admin

### Request body

| Field | Type | Description |
|-------|------|-------------|
| `subjectId` | number | Subject ID |
| `chapterId` | number? | Chapter ID |
| `name` | string | Formula name |
| `formulaLatex` | string | LaTeX formula |
| `description` | string? | Description |
| `variables` | string[]? | Variable names |
| `tags` | string[]? | Tags |

### Update formula

```bash
POST /api/admin/content/formulas/:id/update
```

**Auth**: Admin

### Delete formula

```bash
POST /api/admin/content/formulas/:id/delete
```

**Auth**: Admin

---

## Past Papers Management

### List past papers

```bash
GET /api/admin/content/past-papers
```

**Auth**: Admin

### Query parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `subjectId` | number? | Filter by subject |
| `year` | number? | Filter by year |

### Response

```json
{
  "papers": [
    {
      "id": 1,
      "subjectId": 5,
      "subjectName": "Physics",
      "year": 2024,
      "paperType": "annual",
      "title": "Physics Paper 2024",
      "paperContent": "## Section A\n\n1. Define...",
      "createdAt": "2026-03-20T10:00:00.000Z"
    }
  ],
  "total": 10
}
```

### Create past paper

```bash
POST /api/admin/content/past-papers
```

**Auth**: Admin

### Request body

| Field | Type | Description |
|-------|------|-------------|
| `subjectId` | number | Subject ID |
| `year` | number | Year |
| `paperType` | string | Type (annual, supply) |
| `title` | string | Paper title |
| `paperContent` | string | Markdown content |

### Update past paper

```bash
POST /api/admin/content/past-papers/:id/update
```

**Auth**: Admin

### Delete past paper

```bash
POST /api/admin/content/past-papers/:id/delete
```

**Auth**: Admin

---

## Quiz Questions Management

### List quiz questions

```bash
GET /api/admin/content/quiz-questions?quizId=N
```

**Auth**: Admin

### Query parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `quizId` | number? | Filter by quiz |

### Add quiz question

```bash
POST /api/admin/content/quiz-questions
```

**Auth**: Admin

### Request body

| Field | Type | Description |
|-------|------|-------------|
| `quizId` | number | Quiz ID |
| `question` | string | Question text |
| `optionA` | string | Option A |
| `optionB` | string | Option B |
| `optionC` | string | Option C |
| `optionD` | string | Option D |
| `correctOption` | string | Correct option (a/b/c/d) |
| `explanation` | string? | Explanation |
| `marks` | number | Marks |

### Update quiz question

```bash
POST /api/admin/content/quiz-questions/:id/update
```

**Auth**: Admin

### Delete quiz question

```bash
POST /api/admin/content/quiz-questions/:id/delete
```

**Auth**: Admin

---

## Revision Notes Management

### Get revision notes

```bash
GET /api/admin/content/chapters/:id/revision-notes
```

**Auth**: Admin

### Response

```json
{
  "id": 1,
  "chapterId": 5,
  "content": "## Key Formulas\n\n- F = ma\n\n## Important Definitions\n\n...",
  "createdAt": "2026-03-20T10:00:00.000Z",
  "updatedAt": "2026-03-20T10:00:00.000Z"
}
```

### Update revision notes

```bash
POST /api/admin/content/chapters/:id/revision-notes
```

**Auth**: Admin

### Request body

| Field | Type | Description |
|-------|------|-------------|
| `content` | string | Markdown content (key formulas, definitions, tips) |
